% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-tile.R
\name{tile}
\alias{tile}
\title{Add tile (heatmap) annotations to a sunburst or icicle plot}
\usage{
tile(
  p,
  sb,
  variables,
  y_offset = 0,
  tile_height = 1,
  tile_width = 1,
  colour = "white",
  linewidth = 0,
  show_labels = FALSE,
  label_angle = 90,
  label_size = 3,
  ...
)
}
\arguments{
\item{p}{A ggplot object from \code{sunburst()}, \code{icicle()}, or \code{ggtree()}.}

\item{sb}{The \code{sunburst_data} object used to create \code{p}.}

\item{variables}{Character vector of column names in \code{sb$rects} to
display as tiles.}

\item{y_offset}{Vertical offset from the outermost ring. Default \code{0}.}

\item{tile_height}{Height of each tile band. Default \code{1}.}

\item{tile_width}{Width of each tile. Default \code{1}.}

\item{colour}{Border colour for tiles. Default \code{"white"}.}

\item{linewidth}{Border line width. Default \code{0}.}

\item{show_labels}{Whether to display variable names. Default \code{FALSE}.}

\item{label_angle}{Rotation angle for variable labels. Default \code{90}.}

\item{label_size}{Text size for variable labels. Default \code{3}.}

\item{...}{Passed to \code{geom_tile()}.}
}
\value{
The input ggplot with an additional \code{geom_tile()} layer.
}
\description{
Overlays coloured tiles adjacent to leaf nodes, where each tile
represents a categorical or continuous variable from node attributes.
The fill aesthetic is mapped to the value, allowing users to add
\verb{+ scale_fill_*()} after the call.
}
\note{
Since \code{tile()} maps fill to values, it will conflict with the
fill aesthetic of the base plot. Use a base plot without fill mapping
(e.g., \code{icicle(sb)}) or \code{ggnewscale::new_scale_fill()} before calling.
}
\examples{
df <- data.frame(
  parent = c(NA, "root", "root"),
  child  = c("root", "A", "B"),
  score  = c(NA, 0.5, 0.9)
)
sb <- sunburst_data(df)
p <- icicle(sb)
tile(p, sb, variables = "score")

}
\seealso{
\code{\link[=bars]{bars()}} for bar chart annotations,
\code{\link[=highlight_nodes]{highlight_nodes()}} for node emphasis.
}
