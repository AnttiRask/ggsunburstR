% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-ggtree.R
\name{ggtree}
\alias{ggtree}
\title{Create a tree-style dendrogram plot}
\usage{
ggtree(
  sb,
  colour = "black",
  linewidth = 0.5,
  show_labels = TRUE,
  label_size = 3,
  label_colour = "black",
  rotate = TRUE,
  polar = FALSE,
  blank = TRUE,
  show_scale = FALSE,
  scale_length = 0,
  ...
)
}
\arguments{
\item{sb}{A \code{sunburst_data} object from \code{sunburst_data()}.}

\item{colour}{Line colour for tree segments. Default \code{"black"}.}

\item{linewidth}{Line width for tree segments. Default \code{0.5}.}

\item{show_labels}{Whether to display leaf labels. Default \code{TRUE}.}

\item{label_size}{Text size for leaf labels. Default \code{3}.}

\item{label_colour}{Text colour for leaf labels. Default \code{"black"}.}

\item{rotate}{If \code{TRUE} (default), apply \code{coord_flip()} for horizontal
layout (root left, leaves right).}

\item{polar}{If \code{TRUE}, apply \code{coord_polar()} for circular layout.
Overrides \code{rotate}.}

\item{blank}{If \code{TRUE} (default), apply \code{theme_void()}.}

\item{show_scale}{If \code{TRUE}, display a scale bar indicating
branch-length units. Default \code{FALSE}.}

\item{scale_length}{Length of the scale bar. When \code{0} (default),
auto-computed as one tenth of the total tree depth.}

\item{...}{Passed to \code{geom_segment()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a classical node-link tree diagram (dendrogram) using
\code{geom_segment()} from the segment data in a \code{sunburst_data} object.
}
\note{
The Bioconductor package \code{ggtree} also exports a \code{ggtree()}
function. If both packages are loaded, use \code{ggsunburstR::ggtree()}
to disambiguate.
}
\examples{
sb <- sunburst_data("((a, b, c), (d, e));")
ggtree(sb)
ggtree(sb, polar = TRUE)

}
\seealso{
\code{\link[=sunburst]{sunburst()}} for polar sunburst plots, \code{\link[=icicle]{icicle()}} for
rectangular layouts.
}
