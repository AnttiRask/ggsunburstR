% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multifill.R
\name{sunburst_multifill}
\alias{sunburst_multifill}
\title{Create a sunburst plot with per-depth fill scales}
\usage{
sunburst_multifill(sb, fills, colour = "white", linewidth = 0.2, ...)
}
\arguments{
\item{sb}{A \code{sunburst_data} object from \code{\link[=sunburst_data]{sunburst_data()}}.}

\item{fills}{A named list mapping depth levels (as character strings)
to column names in \code{sb$rects} for fill mapping.
E.g., \code{list("1" = "name", "2" = "value")}.}

\item{colour}{Border colour for rectangles. Default \code{"white"}.}

\item{linewidth}{Border line width. Default \code{0.2}.}

\item{...}{Passed to \code{\link[ggplot2:geom_tile]{ggplot2::geom_rect()}}.}
}
\value{
A \code{ggplot} object with \code{coord_polar()} and \code{theme_void()}.
Users can add \verb{scale_fill_*()} calls after the plot to control
each depth's colour palette.
}
\description{
Creates a sunburst plot where different depth levels can use different
fill colour mappings. Uses \code{ggnewscale::new_scale_fill()} to enable
multiple fill scales in a single plot.
}
\examples{
sb <- sunburst_data("((a, b, c), (d, e));")
if (requireNamespace("ggnewscale", quietly = TRUE)) {
  sunburst_multifill(sb, fills = list("1" = "name", "2" = "name"))
}

}
