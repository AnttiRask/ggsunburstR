% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-sunburst.R
\name{sunburst}
\alias{sunburst}
\title{Create a polar sunburst plot}
\usage{
sunburst(
  sb,
  fill = NULL,
  colour = "white",
  linewidth = 0.2,
  show_labels = FALSE,
  label_type = c("radial", "perpendicular"),
  ...
)
}
\arguments{
\item{sb}{A \code{sunburst_data} object from \code{sunburst_data()}.}

\item{fill}{Column name in \code{sb$rects} to map to fill aesthetic. When
\code{NULL}, a static grey fill is used.}

\item{colour}{Border colour for rectangles. Default \code{"white"}.}

\item{linewidth}{Border line width. Default \code{0.2}.}

\item{show_labels}{Whether to add text labels for leaf nodes.
Default \code{FALSE}.}

\item{label_type}{Label orientation. \code{"radial"}: text reads outward.
\code{"perpendicular"}: text follows arc (post-MVP quality).}

\item{...}{Passed to \code{geom_rect()}.}
}
\value{
A \code{ggplot} object with \code{coord_polar()} and \code{theme_void()}.
}
\description{
Creates a sunburst (radial) plot from a \code{sunburst_data} object using
\code{ggplot2::geom_rect()} with \code{coord_polar()} and \code{theme_void()}.
}
\examples{
sb <- sunburst_data("((a, b, c), (d, e));")
sunburst(sb)
sunburst(sb, fill = "depth")

}
