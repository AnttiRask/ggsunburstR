% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sunburst-data.R
\name{sunburst_data}
\alias{sunburst_data}
\title{Parse hierarchical input into sunburst/icicle data}
\usage{
sunburst_data(
  input,
  type = "auto",
  values = NULL,
  branchvalues = c("remainder", "total"),
  leaf_mode = c("actual", "extended"),
  ladderize = FALSE,
  ultrametric = FALSE,
  xlim = 360,
  rot = 0,
  node_attributes = NULL,
  sep = NULL,
  ...
)
}
\arguments{
\item{input}{Hierarchical data. One of: Newick string, file path,
data.frame with parent-child columns, \code{ape::phylo} object,
\code{data.tree::Node} object, character vector of delimited paths,
or data.frame with a \code{path} column.}

\item{type}{Input type. One of \code{"auto"}, \code{"newick"}, \code{"phylo"},
\code{"datatree"}, \code{"paths"}, \code{"lineage"}, \code{"node_parent"}, \code{"dataframe"}.
Auto-detection is recommended.}

\item{values}{Column name (character, for data.frame input) or named
numeric vector mapping node names to values for sector sizing.
\code{NULL} for equal weight.}

\item{branchvalues}{How parent values relate to children.
\code{"remainder"}: parent value is additive.
\code{"total"}: parent value equals sum of children.}

\item{leaf_mode}{How short branches are handled.
\code{"actual"}: stop at real depth.
\code{"extended"}: extend to max depth.}

\item{ladderize}{Sort partitions by descendant count. \code{FALSE} for
no sorting, \code{TRUE} or \code{"right"} for ascending, \code{"left"} for descending.}

\item{ultrametric}{If \code{TRUE}, convert tree to ultrametric topology.}

\item{xlim}{Angular span in degrees. Default \code{360} for full circle.}

\item{rot}{Rotation offset in degrees.}

\item{node_attributes}{Character vector of additional node attribute
names to include in output.}

\item{sep}{Separator for file-based inputs.}

\item{...}{Reserved for future parameters.}
}
\value{
An S3 object of class \code{"sunburst_data"} containing \verb{$rects},
\verb{$leaf_labels}, \verb{$node_labels}, \verb{$segments}, and \verb{$tree}.
}
\description{
Accepts hierarchical data in multiple formats (Newick strings/files,
data frames, lineage files, node-parent files), computes rectangle
coordinates for each node, and returns a \code{sunburst_data} S3 object
suitable for rendering with \code{sunburst()} or \code{icicle()}.
}
\examples{
# Newick string
sb <- sunburst_data("((a, b, c), (d, e));")
sb

# Data frame
df <- data.frame(
  parent = c(NA, "root", "root"),
  child  = c("root", "A", "B")
)
sb <- sunburst_data(df)

}
