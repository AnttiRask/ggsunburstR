% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-sunburst.R
\name{geom_sunburst}
\alias{geom_sunburst}
\title{Sunburst layer for ggplot2}
\usage{
geom_sunburst(
  mapping = NULL,
  data = NULL,
  stat = "sunburst",
  position = "identity",
  ...,
  colour = "white",
  linewidth = 0.2,
  values = NULL,
  branchvalues = "remainder",
  leaf_mode = "actual",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. \strong{Required:} \code{aes(id, parent)}.
Map any node-identifier column to \code{id} (e.g., \code{aes(id = child)})
and the parent column to \code{parent}. The root row should have
\code{parent = NA}. Optional: \code{fill}, \code{colour}, \code{alpha}.}

\item{data}{A data.frame with at least \code{id} (or \code{child}/\code{node}) and
\code{parent} columns. Extra columns are available for aesthetic mapping.}

\item{stat}{The statistical transformation. Default \code{"sunburst"}.}

\item{position}{Position adjustment. Default \code{"identity"}.}

\item{...}{Other arguments passed to the layer.}

\item{colour}{Border colour for rectangles. Default \code{"white"}.}

\item{linewidth}{Border line width. Default \code{0.2}.}

\item{values}{Column name (string) for value-weighted sizing.
\code{NULL} for equal weight. Default \code{NULL}.}

\item{branchvalues}{How parent values relate to children.
\code{"remainder"} (default) or \code{"total"}.}

\item{leaf_mode}{How short branches are handled.
\code{"actual"} (default) or \code{"extended"}.}

\item{na.rm}{If \code{FALSE}, missing values produce warnings. Default \code{FALSE}.}

\item{show.legend}{Logical. Include this layer in legends?}

\item{inherit.aes}{If \code{TRUE}, inherit aesthetics from \code{ggplot()}.}
}
\value{
A ggplot2 layer.
}
\description{
Creates a sunburst (or icicle) layer from a parent-child data.frame.
Uses \code{StatSunburst} to convert the hierarchy into rectangle coordinates
and \code{GeomRect} to render them.
}
\details{
Add \code{coord_polar()} for a sunburst layout, or leave Cartesian for an
icicle layout. Fill mapping works via standard ggplot2 aesthetics.
}
\examples{
df <- data.frame(
  parent = c(NA, "root", "root", "A", "A"),
  child  = c("root", "A", "B", "a1", "a2"),
  group  = c("r", "g1", "g2", "g1", "g1")
)

# Icicle (Cartesian)
ggplot2::ggplot(df) +
  geom_sunburst(ggplot2::aes(id = child, parent = parent, fill = group))

# Sunburst (polar)
ggplot2::ggplot(df) +
  geom_sunburst(ggplot2::aes(id = child, parent = parent, fill = group)) +
  ggplot2::coord_polar()

}
\seealso{
\code{\link[=sunburst]{sunburst()}}, \code{\link[=icicle]{icicle()}} for the convenience-function API.
}
