% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-donut.R
\name{donut}
\alias{donut}
\title{Create a donut (ring) chart}
\usage{
donut(
  sb,
  levels = 1,
  fill = NULL,
  colour = "white",
  linewidth = 0.2,
  show_labels = FALSE,
  hole_size = 1,
  ...
)
}
\arguments{
\item{sb}{A \code{sunburst_data} object from \code{sunburst_data()}.}

\item{levels}{Number of depth levels to display (from the outermost
inward). \code{1} = single ring, \code{2} = two concentric rings.}

\item{fill}{Column name to map to fill aesthetic. \code{NULL} for static grey.}

\item{colour}{Border colour for segments. Default \code{"white"}.}

\item{linewidth}{Border line width. Default \code{0.2}.}

\item{show_labels}{Whether to display labels. Default \code{FALSE}.}

\item{hole_size}{Size of the centre hole. Higher = larger hole relative
to ring thickness. Default \code{1}.}

\item{...}{Passed to \code{geom_rect()}.}
}
\value{
A \code{ggplot} object with \code{coord_polar()} and \code{theme_void()}.
}
\description{
Creates a donut chart — a sunburst restricted to the outermost 1–N
depth levels. The centre hole is created by shifting Y coordinates
so the innermost displayed ring has \code{ymin > 0}.
}
\examples{
sb <- sunburst_data("((a, b, c), (d, e));")
donut(sb, fill = "name")
donut(sb, levels = 2, fill = "depth")

}
