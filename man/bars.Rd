% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-bars.R
\name{bars}
\alias{bars}
\title{Add bar chart annotations to a sunburst or icicle plot}
\usage{
bars(
  p,
  sb,
  variables,
  y_offset = 0,
  bar_height = 1,
  box_colour = "black",
  bar_colour = "black",
  show_labels = FALSE,
  show_values = FALSE,
  label_size = 3,
  value_size = 2.5,
  ...
)
}
\arguments{
\item{p}{A ggplot object from \code{sunburst()}, \code{icicle()}, or \code{ggtree()}.}

\item{sb}{The \code{sunburst_data} object used to create \code{p}.}

\item{variables}{Character vector of numeric column names in \code{sb$rects}
to display as bars.}

\item{y_offset}{Vertical offset from the outermost ring. Default \code{0}.}

\item{bar_height}{Height of each bar band. Default \code{1}.}

\item{box_colour}{Outline colour for the outer box. Default \code{"black"}.}

\item{bar_colour}{Fill colour for the inner value bar. Default \code{"black"}.}

\item{show_labels}{Whether to display variable names. Default \code{FALSE}.}

\item{show_values}{Whether to display numeric values inside bars.
Default \code{FALSE}.}

\item{label_size}{Text size for variable labels. Default \code{3}.}

\item{value_size}{Text size for value labels. Default \code{2.5}.}

\item{...}{Passed to the outer \code{geom_rect()}.}
}
\value{
The input ggplot with additional bar layers.
}
\description{
Overlays bar charts adjacent to leaf nodes, where each bar represents
a quantitative variable from node attributes. Values are max-normalised
per variable to the 0--1 range.
}
\examples{
df <- data.frame(
  parent = c(NA, "root", "root"),
  child  = c("root", "A", "B"),
  score  = c(NA, 0.5, 0.9)
)
sb <- sunburst_data(df)
p <- icicle(sb, fill = "depth")
bars(p, sb, variables = "score")

}
\seealso{
\code{tile()} for heatmap-style annotations,
\code{\link[=highlight_nodes]{highlight_nodes()}} for node emphasis.
}
