% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-icicle.R
\name{icicle}
\alias{icicle}
\title{Create a rectangular icicle plot}
\usage{
icicle(
  sb,
  fill = NULL,
  colour = "white",
  linewidth = 0.2,
  show_labels = FALSE,
  show_node_labels = FALSE,
  label_size = 3,
  min_label_angle = 0,
  ...
)
}
\arguments{
\item{sb}{A \code{sunburst_data} object from \code{sunburst_data()}.}

\item{fill}{Column name in \code{sb$rects} to map to fill aesthetic. When
\code{NULL}, a static grey fill is used.}

\item{colour}{Border colour for rectangles. Default \code{"white"}.}

\item{linewidth}{Border line width. Default \code{0.2}.}

\item{show_labels}{Whether to add text labels. Default \code{FALSE}.}

\item{show_node_labels}{Whether to add text labels for internal nodes.
Only takes effect when \code{show_labels = TRUE}. Default \code{FALSE}.}

\item{label_size}{Text size for labels. Default \code{3}.}

\item{min_label_angle}{Minimum angular extent (degrees) for a node to
receive a label. Nodes with \code{delta_angle < min_label_angle} are not
labelled. Default \code{0} (no filtering).}

\item{...}{Passed to \code{geom_rect()}.}
}
\value{
A \code{ggplot} object with \code{scale_y_reverse()} and \code{theme_void()}.
}
\description{
Creates an icicle (rectangular, top-down) plot from a \code{sunburst_data}
object using \code{ggplot2::geom_rect()} with \code{scale_y_reverse()} and
\code{theme_void()}.
}
\examples{
sb <- sunburst_data("((a, b, c), (d, e));")
icicle(sb)
icicle(sb, fill = "depth")

}
