% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drilldown.R
\name{drilldown}
\alias{drilldown}
\title{Drill down into a subtree}
\usage{
drilldown(sb, node, ...)
}
\arguments{
\item{sb}{A \code{sunburst_data} object.}

\item{node}{Node to use as the new root. Character string (node name)
or integer (node ID).}

\item{...}{Override parameters for the recomputation (e.g., \code{xlim},
\code{rot}).}
}
\value{
A new \code{sunburst_data} object rooted at the selected node.
}
\description{
Extracts a subtree rooted at the specified node and recomputes
coordinates so the subtree fills the full angular space. The result
is a new \code{sunburst_data} object compatible with all plot functions.
}
\examples{
sb <- sunburst_data("((a, b)X, (c, d)Y)root;")
# Drill into the X subtree (containing a, b)
sub <- drilldown(sb, node = "X")
sunburst(sub, fill = "name")

}
\seealso{
\code{\link[=sunburst_data]{sunburst_data()}} for creating the initial data,
\code{\link[=sunburst]{sunburst()}}, \code{\link[=icicle]{icicle()}}, \code{\link[=donut]{donut()}} for plotting.
}
